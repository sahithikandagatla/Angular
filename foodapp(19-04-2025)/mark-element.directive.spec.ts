import { MarkElementDirective } from './mark-element.directive';

describe('MarkElementDirective', () => {
  it('should create an instance', () => {
    const directive = new MarkElementDirective();
    expect(directive).toBeTruthy();
  });
});
